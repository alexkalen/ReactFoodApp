import styles from "./HeaderCartButton";

function Header(props) {

    return(
        <header className={styles.header}>
            <h1>FoodApp</h1>
        </header>
    );

}

export default Header;